import { GetTimeLeftPipe } from './get-time-left.pipe';

describe('GetTimeLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTimeLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
