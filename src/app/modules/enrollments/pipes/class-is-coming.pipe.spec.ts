import { ClassIsComingPipe } from './class-is-coming.pipe';

describe('ClassIsComingPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassIsComingPipe();
    expect(pipe).toBeTruthy();
  });
});
